{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <main class="main-content">
        <div class="fullwidth-block">
		</div> <!-- .fullwidth-block -->

        <div class="fullwidth-block">
            <div class="container">
                <div class="row">
                        <div class="card-body">
                            <h1 class="tomato-color">Country</h1>
                            {{ form_start(countryForm) }}
                                {{ form_row(countryForm.name) }}
                                {{ form_row(countryForm.iso3) }}
                                {{ form_row(countryForm.createdAt) }}
                                {{ form_row(countryForm.isActive) }}
                                <button type="submit" class="btn">Udpate</button>
                            {{ form_end(countryForm) }}
                        </div>
                    </div>
                    </div>
                </div> <!-- .row -->
            </div> <!-- .container -->
        </div> <!-- .fullwidth-block -->
    </main> <!-- .main-content -->
{% endblock %}
