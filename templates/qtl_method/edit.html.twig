{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <main class="main-content">
        <div class="fullwidth-block">
		</div> <!-- .fullwidth-block -->

        <div class="fullwidth-block">
            <div class="container">
                <div class="row">
                        <div class="card-body">
                            <h1 class="tomato-color">QTL Method Edition</h1>
                            {{ form_start(qtlMethodForm) }}
                                {{ form_row(qtlMethodForm.name) }}
                                {{ form_row(qtlMethodForm.parentTerm) }}
                                {{ form_row(qtlMethodForm.ontology_id) }}
                                {{ form_row(qtlMethodForm.createdAt) }}
                                {{ form_row(qtlMethodForm.isActive) }}
                                <button type="submit" class="btn">Udpate</button>
                            {{ form_end(qtlMethodForm) }}
                        </div>
                    </div>
                    </div>
                </div> <!-- .row -->
            </div> <!-- .container -->
        </div> <!-- .fullwidth-block -->
    </main> <!-- .main-content -->
{% endblock %}
