{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <main class="main-content">
        <div class="fullwidth-block">
		</div> <!-- .fullwidth-block -->

        <div class="fullwidth-block">
            <div class="container">
                <div class="row">
                        <div class="card-body">
                            <h1 class="tomato-color">QTL Method Creation</h1>
                            {{ form_start(qtlMethodForm) }}
                                <div class="form-group">
                                    {{ form_row(qtlMethodForm.ontology_id, {'attr': {'placeholder' : 'Enter the ontology id'}}) }}
                                </div>
                                <div class="form-group">
                                    {{ form_row(qtlMethodForm.parentTerm, {'attr': {'placeholder' : 'Enter the parent term'}}) }} 
                                </div>
                                <div class="form-group">
                                    {{ form_row(qtlMethodForm.name, {'attr': {'placeholder' : 'Enter the name'}}) }} 
                                </div>
                                <div class="form-group">
                                    <button type="submit" class="btn btn-outline-primary">Create</button>
                                    <button type="reset" class="btn btn-outline-secondary">Cancel</button>
                                </div>
                            {{ form_end(qtlMethodForm) }}
                        </div>
                    </div>
                    </div>
                </div> <!-- .row -->
            </div> <!-- .container -->
        </div> <!-- .fullwidth-block -->
    </main> <!-- .main-content -->
{% endblock %}
