{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <main class="main-content">
        <div class="fullwidth-block">
		</div> <!-- .fullwidth-block -->
        <div class="fullwidth-block">
            <div class="container">
                <div class="row">
                        <div class="card-body">
                            {% if structureMethods | length > 0 %}
                                <h1 class="tomato-color">Structure Method List</h1>
                                <table class="table table-striped">
                                <thead>
                                    <tr>
                                    <th scope="col">Name</th>
                                    <th scope="col">Ontology ID</th>
                                    <th scope="col">Action</th>
                                    </tr>
                                </thead>
                                <tbody>
                                {% for structureMethod in structureMethods %}
                                    <tr>
                                        <td><a href="{{ path('structure_method_details', {id: structureMethod.id}) }}">{{ structureMethod.name }}</a></td>
                                        <td><a href="{{ path('structure_method_details', {id: structureMethod.id}) }}">{{ structureMethod.ontologyID }}</a></td>
                                        <td>
                                            <a href="{{ path('structure_method_details', {id: structureMethod.id}) }}"><button class="btn btn-outline-primary">View</button></a>
                                        </td>
                                    </tr>
                                {% endfor %}
                            {% else %} 
                                <p class="tomato-color">No structure method has been provided in our system</p>
                            {% endif %}
                        </tbody>
                        </table>
                     
                        </div>
                    </div>
                    </div>
                </div> <!-- .row -->
            </div> <!-- .container -->
        </div> <!-- .fullwidth-block -->
    </main> <!-- .main-content -->
{% endblock %}
