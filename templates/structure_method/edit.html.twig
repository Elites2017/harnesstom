{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <main class="main-content">
        <div class="fullwidth-block">
		</div> <!-- .fullwidth-block -->

        <div class="fullwidth-block">
            <div class="container">
                <div class="row">
                        <div class="card-body">
                            <h1 class="tomato-color">Structure Method Edition</h1>
                            {{ form_start(structureMethodForm) }}
                                {{ form_row(structureMethodForm.name) }}
                                {{ form_row(structureMethodForm.parentTerm) }}
                                {{ form_row(structureMethodForm.ontology_id) }}
                                {{ form_row(structureMethodForm.createdAt) }}
                                {{ form_row(structureMethodForm.isActive) }}
                                <button type="submit" class="btn">Udpate</button>
                            {{ form_end(structureMethodForm) }}
                        </div>
                    </div>
                    </div>
                </div> <!-- .row -->
            </div> <!-- .container -->
        </div> <!-- .fullwidth-block -->
    </main> <!-- .main-content -->
{% endblock %}
