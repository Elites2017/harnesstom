{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <main class="main-content">
        <div class="fullwidth-block">
		</div> <!-- .fullwidth-block -->

        <div class="fullwidth-block">
            <div class="container">
                <div class="row">
                        <div class="card-body">
                            <h1 class="tomato-color">Enzyme</h1>
                            {{ form_start(enzymeForm) }}
                                {{ form_row(enzymeForm.name) }}
                                {{ form_row(enzymeForm.createdAt) }}
                                {{ form_row(enzymeForm.isActive) }}
                                <button type="submit" class="btn">Udpate</button>
                            {{ form_end(enzymeForm) }}
                        </div>
                    </div>
                    </div>
                </div> <!-- .row -->
            </div> <!-- .container -->
        </div> <!-- .fullwidth-block -->
    </main> <!-- .main-content -->
{% endblock %}
