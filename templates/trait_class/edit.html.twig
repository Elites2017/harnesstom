{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <main class="main-content">
        <div class="fullwidth-block">
		</div> <!-- .fullwidth-block -->

        <div class="fullwidth-block">
            <div class="container">
                <div class="row">
                        <div class="card-body">
                            <h1 class="tomato-color">Trait Edition</h1>
                            {{ form_start(traitClassForm) }}
                                {{ form_row(traitClassForm.name) }}
                                {{ form_row(traitClassForm.description) }}
                                {{ form_row(traitClassForm.parentTerm) }}
                                {{ form_row(traitClassForm.ontology_id) }}
                                {{ form_row(traitClassForm.createdAt) }}
                                {{ form_row(traitClassForm.isActive) }}
                                <button type="submit" class="btn">Udpate</button>
                            {{ form_end(traitClassForm) }}
                        </div>
                    </div>
                    </div>
                </div> <!-- .row -->
            </div> <!-- .container -->
        </div> <!-- .fullwidth-block -->
    </main> <!-- .main-content -->
{% endblock %}
