{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <main class="main-content">
        <div class="fullwidth-block">
		</div> <!-- .fullwidth-block -->

        <div class="fullwidth-block">
            <div class="container">
                <div class="row">
                        <div class="card-body">
                            <h1 class="tomato-color">Ci Criteria</h1>
                            {{ form_start(ciCriteriaForm) }}
                                <div class="form-group">
                                    {{ form_row(ciCriteriaForm.ontology_id, {'attr': {'placeholder' : 'Enter the the ontology id'}}) }}
                                </div>
                                <div class="form-group">
                                    {{ form_row(ciCriteriaForm.parentTerm, {'attr': {'placeholder' : 'Enter the the parent term'}}) }} 
                                </div>
                                <div class="form-group">
                                    {{ form_row(ciCriteriaForm.name, {'attr': {'placeholder' : 'Enter the the ci criteria name'}}) }} 
                                </div>
                                <div class="form-group">
                                    <button type="submit" class="btn btn-outline-primary">Create</button>
                                    <button type="reset" class="btn btn-outline-secondary">Cancel</button>
                                </div>
                            {{ form_end(ciCriteriaForm) }}
                        </div>
                    </div>
                    </div>
                </div> <!-- .row -->
            </div> <!-- .container -->
        </div> <!-- .fullwidth-block -->
    </main> <!-- .main-content -->
{% endblock %}
