{% extends 'base.html.twig' %}

{% form_theme metaboliteClassForm _self %}

{% block datalist_widget %}
    <input {{ block('widget_attributes') }} list="metabolite_class_parentTerm_list" value="{{ form.vars.value }}" />
    <datalist id="metabolite_class_parentTerm_list">
        {% for choice in choices %}
            <option data-value="{{ choice.value }}">
                {{ choice.label }}
            </option>
        {% endfor %}
    <input type="hidden" name="metabolite_class[parentTerm]" id="metabolite_class_parentTerm_hidden">
    </datalist>
{% endblock %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
	<div class="container-fluid">
		<div class="row">
			<div class="col-xl-12 col-lg-11">
				{% for category, messages in app.flashes(['success', 'warning', 'danger']) %}
					{% for message in messages %}
						<div class="alert alert-{{ category }} alert-dismissible fade show" role="alert">{{ message }}
							<button type="button" class="close" data-dismiss="alert" aria-label="Close">
								<span aria-hidden="true">&times;</span>
							</button>
						</div>
					{% endfor %}
				{% endfor %}
				<div class="card shadow mb-4">
					<!-- Card Header - Dropdown -->
					<div
						class="card-header py-3 flex-row align-items-center justify-content-between">
						<h4 class="m-0 font-weight-bold text-center tomato-color">New Metabolite Class</h4>
					</div>
					<!-- Card Body -->
					<div class="card-body">
						{{ form_start(metaboliteClassForm) }}
						<div class="row">
							<div class="col-md-4 mb-3">
								{{ form_row(metaboliteClassForm.ontology_id, {'attr': {'placeholder' : 'Enter the ontology id'}}) }}
							</div>

							<div class="col-md-4 mb-3">
								{{ form_row(metaboliteClassForm.parentTerm, {'attr': {'placeholder' : 'Enter the parent term', 'class': "custom-select d-block w-100"}}) }}
							</div>

							<div class="col-md-4 mb-3">
								{{ form_row(metaboliteClassForm.name, {'attr': {'placeholder' : 'Enter the metabolite class name'}}) }}
							</div>
						</div>

						<div class="row">
							<div class="col-md-12 mb-3">
								{{ form_row(metaboliteClassForm.description, {'attr': {'placeholder' : 'Enter the metabolite class description'}}) }}
							</div>
						</div>
						<div class="row">
							{% include 'resource/btn_create.html.twig' %}
						</div>
						{{ form_end(metaboliteClassForm) }}
					</div>
				</div>
			</div>
		</div>
		<!-- .row -->
	</div>
	<!-- .container -->
{% endblock %}

{% block javascripts %}
    {% include 'resource/create_edit_js.html.twig' %}
{% endblock %}