{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <main class="main-content">
        <div class="fullwidth-block">
		</div> <!-- .fullwidth-block -->
        <div class="fullwidth-block">
            <div class="container">
                <div class="row">
                        <div class="card-body">
                            {% if traitProcessings | length > 0 %}
                                <h1 class="tomato-color">Trait Processing List</h1>
                                <table class="table table-striped">
                                <thead>
                                    <tr>
                                    <th scope="col">Name</th>
                                    <th scope="col">Description</th>
                                    <th scope="col">Action</th>
                                    </tr>
                                </thead>
                                <tbody>
                                {% for traitProcessing in traitProcessings %}
                                    <tr>
                                        <td><a href="{{ path('trait_processing_details', {id: traitProcessing.id}) }}">{{ traitProcessing.name }}</a></td>
                                        <td><a href="{{ path('trait_processing_details', {id: traitProcessing.id}) }}">{{ traitProcessing.description }}</a></td>
                                        <td>
                                            <a href="{{ path('trait_processing_details', {id: traitProcessing.id}) }}"><button class="btn btn-outline-primary">View</button></a>
                                        </td>
                                    </tr>
                                {% endfor %}
                            {% else %} 
                                <p class="tomato-color">No trait processing has been provided in our system</p>
                            {% endif %}
                        </tbody>
                        </table>
                     
                        </div>
                    </div>
                    </div>
                </div> <!-- .row -->
            </div> <!-- .container -->
        </div> <!-- .fullwidth-block -->
    </main> <!-- .main-content -->
{% endblock %}
