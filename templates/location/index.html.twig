{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <main class="main-content">
        <div class="fullwidth-block">
		</div> <!-- .fullwidth-block -->
        <div class="fullwidth-block">
            <div class="container">
                <div class="row">
                        <div class="card-body">
                            {% if locations | length > 0 %}
                                <h1 class="tomato-color">Location List</h1>
                                <table class="table table-striped">
                                <thead>
                                    <tr>
                                    <th scope="col">Name</th>
                                    <th scope="col">Longitude</th>
                                    <th scope="col">Latitude</th>
                                    <th scope="col">Altitude</th>
                                    <th scope="col">Action</th>
                                    </tr>
                                </thead>
                                <tbody>
                                {% for location in locations %}
                                    <tr>
                                        <td><a href="{{ path('location_details', {id: location.id}) }}">{{ location.name }}</a></td>
                                        <td><a href="{{ path('location_details', {id: location.id}) }}">{{ location.longitudeCo }}</a></td>
                                        <td><a href="{{ path('location_details', {id: location.id}) }}">{{ location.latitudeCo }}</a></td>
                                        <td><a href="{{ path('location_details', {id: location.id}) }}">{{ location.altitudeCo }}</a></td>
                                        <td>
                                            <a href="{{ path('location_details', {id: location.id}) }}"><button class="btn btn-outline-primary">View</button></a>
                                        </td>
                                    </tr>
                                {% endfor %}
                            {% else %} 
                                <p class="tomato-color">No location has been provided in our system</p>
                            {% endif %}
                        </tbody>
                        </table>
                     
                        </div>
                    </div>
                    </div>
                </div> <!-- .row -->
            </div> <!-- .container -->
        </div> <!-- .fullwidth-block -->
    </main> <!-- .main-content -->
{% endblock %}
