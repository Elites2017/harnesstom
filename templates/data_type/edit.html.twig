{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <main class="main-content">
        <div class="fullwidth-block">
		</div> <!-- .fullwidth-block -->

        <div class="fullwidth-block">
            <div class="container">
                <div class="row">
                        <div class="card-body">
                            <h1 class="tomato-color">Data Type</h1>
                            {{ form_start(dataTypeForm) }}
                                {{ form_row(dataTypeForm.label) }}
                                {{ form_row(dataTypeForm.createdAt) }}
                                {{ form_row(dataTypeForm.isActive) }}
                                <button type="submit" class="btn">Udpate</button>
                            {{ form_end(dataTypeForm) }}
                        </div>
                    </div>
                    </div>
                </div> <!-- .row -->
            </div> <!-- .container -->
        </div> <!-- .fullwidth-block -->
    </main> <!-- .main-content -->
{% endblock %}
