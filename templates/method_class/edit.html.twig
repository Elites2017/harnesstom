{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <main class="main-content">
        <div class="fullwidth-block">
		</div> <!-- .fullwidth-block -->

        <div class="fullwidth-block">
            <div class="container">
                <div class="row">
                        <div class="card-body">
                            <h1 class="tomato-color">Method Class Udpate</h1>
                            {{ form_start(methodClassForm) }}
                                {{ form_row(methodClassForm.name) }}
                                {{ form_row(methodClassForm.description) }}
                                {{ form_row(methodClassForm.createdAt) }}
                                {{ form_row(methodClassForm.isActive) }}
                                <button type="submit" class="btn">Udpate</button>
                            {{ form_end(methodClassForm) }}
                        </div>
                    </div>
                    </div>
                </div> <!-- .row -->
            </div> <!-- .container -->
        </div> <!-- .fullwidth-block -->
    </main> <!-- .main-content -->
{% endblock %}
