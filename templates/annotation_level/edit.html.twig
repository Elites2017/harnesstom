{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <main class="main-content">
        <div class="fullwidth-block">
		</div> <!-- .fullwidth-block -->

        <div class="fullwidth-block">
            <div class="container">
                <div class="row">
                        <div class="card-body">
                            <h1 class="tomato-color">Annotation Level</h1>
                            {{ form_start(annotationLevelForm) }}
                                {{ form_row(annotationLevelForm.label) }}
                                {{ form_row(annotationLevelForm.code) }}
                                {{ form_row(annotationLevelForm.createdAt) }}
                                {{ form_row(annotationLevelForm.isActive) }}
                                <button type="submit" class="btn">Udpate</button>
                            {{ form_end(annotationLevelForm) }}
                        </div>
                    </div>
                    </div>
                </div> <!-- .row -->
            </div> <!-- .container -->
        </div> <!-- .fullwidth-block -->
    </main> <!-- .main-content -->
{% endblock %}
