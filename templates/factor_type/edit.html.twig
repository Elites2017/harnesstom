{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <main class="main-content">
        <div class="fullwidth-block">
		</div> <!-- .fullwidth-block -->

        <div class="fullwidth-block">
            <div class="container">
                <div class="row">
                        <div class="card-body">
                            <h1 class="tomato-color">Factor Type</h1>
                            {{ form_start(factorTypeForm) }}
                                {{ form_row(factorTypeForm.name) }}
                                {{ form_row(factorTypeForm.description) }}
                                {{ form_row(factorTypeForm.parentTerm) }}
                                {{ form_row(factorTypeForm.ontology_id) }}
                                {{ form_row(factorTypeForm.createdAt) }}
                                {{ form_row(factorTypeForm.isActive) }}
                                <button type="submit" class="btn">Udpate</button>
                            {{ form_end(factorTypeForm) }}
                        </div>
                    </div>
                    </div>
                </div> <!-- .row -->
            </div> <!-- .container -->
        </div> <!-- .fullwidth-block -->
    </main> <!-- .main-content -->
{% endblock %}
