{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <main class="main-content">
        <div class="fullwidth-block">
		</div> <!-- .fullwidth-block -->
        <div class="fullwidth-block">
            <div class="container">
                <div class="row">
                        <div class="card-body">
                            {% if factor_types | length > 0 %}
                                <h1 class="tomato-color">factorTypes list</h1>
                                <table class="table table-striped">
                                <thead>
                                    <tr>
                                    <th scope="col">Name</th>
                                    <th scope="col">Description</th>
                                    <th scope="col">Action</th>
                                    </tr>
                                </thead>
                                <tbody>
                                {% for factor_type in factor_types %}
                                    <tr>
                                        <td><a href="{{ path('factor_type_details', {id: factor_type.id}) }}">{{ factor_type.name }}</a></td>
                                        <td><a href="{{ path('factor_type_details', {id: factor_type.id}) }}">{{ factor_type.description }}</a></td>
                                        <td>
                                            <a href="{{ path('factor_type_details', {id: factor_type.id}) }}"><button class="btn btn-outline-primary">View</button></a>
                                        </td>
                                    </tr>
                                {% endfor %}
                            {% else %} 
                                <p class="tomato-color">No factorType has been provided in our system</p>
                            {% endif %}
                        </tbody>
                        </table>
                     
                        </div>
                    </div>
                    </div>
                </div> <!-- .row -->
            </div> <!-- .container -->
        </div> <!-- .fullwidth-block -->
    </main> <!-- .main-content -->
{% endblock %}
