{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <main class="main-content">
        <div class="fullwidth-block">
		</div> <!-- .fullwidth-block -->
        <div class="fullwidth-block">
            <div class="container">
                <div class="row">
                        <div class="card-body">
                            {% if observationVariables | length > 0 %}
                                <h1 class="tomato-color">Observation Variable List</h1>
                                <table class="table table-striped">
                                <thead>
                                    <tr>
                                    <th scope="col">Name</th>
                                    <th scope="col">Main Abbreviation</th>
                                    <th scope="col">Description</th>
                                    <th scope="col">Trait</th>
                                    <th scope="col">Action</th>
                                    </tr>
                                </thead>
                                <tbody>
                                {% for observationVariable in observationVariables %}
                                    <tr>
                                        <td><a href="{{ path('observation_variable_details', {id: observationVariable.id}) }}">{{ observationVariable.name }}</a></td>
                                        <td><a href="{{ path('observation_variable_details', {id: observationVariable.id}) }}">{{ observationVariable.mainAbbreviaition }}</a></td>
                                        <td><a href="{{ path('observation_variable_details', {id: observationVariable.id}) }}">{{ observationVariable.description }}</a></td>
                                        <td><a href="{{ path('observation_variable_details', {id: observationVariable.id}) }}">{{ observationVariable.trait }}</a></td>
                                        <td>
                                            <a href="{{ path('observation_variable_details', {id: observationVariable.id}) }}"><button class="btn btn-outline-primary">View</button></a>
                                        </td>
                                    </tr>
                                {% endfor %}
                            {% else %} 
                                <p class="tomato-color">No observation variable has been provided in our system</p>
                            {% endif %}
                        </tbody>
                        </table>
                     
                        </div>
                    </div>
                    </div>
                </div> <!-- .row -->
            </div> <!-- .container -->
        </div> <!-- .fullwidth-block -->
    </main> <!-- .main-content -->
{% endblock %}
