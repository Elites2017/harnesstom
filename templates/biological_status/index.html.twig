{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <main class="main-content">
        <div class="fullwidth-block">
		</div> <!-- .fullwidth-block -->
        <div class="fullwidth-block">
            <div class="container">
                <div class="row">
                        <div class="card-body">
                            {% if biologicalStatuses | length > 0 %}
                                <h1 class="tomato-color">Biological Status List</h1>
                                <table class="table table-striped">
                                <thead>
                                    <tr>
                                    <th scope="col">Label</th>
                                    <th scope="col">Code</th>
                                    <th scope="col">Parent Term</th>
                                    <th scope="col">Action</th>
                                    </tr>
                                </thead>
                                <tbody>
                                {% for biologicalStatus in biologicalStatuses %}
                                    <tr>
                                        <td><a href="{{ path('biological_status_details', {id: biologicalStatus.id}) }}">{{ biologicalStatus.label }}</a></td>
                                        <td><a href="{{ path('biological_status_details', {id: biologicalStatus.id}) }}">{{ biologicalStatus.code }}</a></td>
                                        <td><a href="{{ path('biological_status_details', {id: biologicalStatus.id}) }}">{{ biologicalStatus.parentTerm }}</a></td>
                                        <td>
                                            <a href="{{ path('biological_status_details', {id: biologicalStatus.id}) }}"><button class="btn btn-outline-primary">View</button></a>
                                        </td>
                                    </tr>
                                {% endfor %}
                            {% else %} 
                                <p class="tomato-color">No biological status has been provided in our system</p>
                            {% endif %}
                        </tbody>
                        </table>
                     
                        </div>
                    </div>
                    </div>
                </div> <!-- .row -->
            </div> <!-- .container -->
        </div> <!-- .fullwidth-block -->
    </main> <!-- .main-content -->
{% endblock %}
