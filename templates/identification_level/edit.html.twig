{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <main class="main-content">
        <div class="fullwidth-block">
		</div> <!-- .fullwidth-block -->

        <div class="fullwidth-block">
            <div class="container">
                <div class="row">
                        <div class="card-body">
                            <h1 class="tomato-color">Identification Level Edition</h1>
                            {{ form_start(identificationLevelForm) }}
                                {{ form_row(identificationLevelForm.label) }}
                                {{ form_row(identificationLevelForm.code) }}
                                {{ form_row(identificationLevelForm.createdAt) }}
                                {{ form_row(identificationLevelForm.isActive) }}
                                <button type="submit" class="btn">Udpate</button>
                            {{ form_end(identificationLevelForm) }}
                        </div>
                    </div>
                    </div>
                </div> <!-- .row -->
            </div> <!-- .container -->
        </div> <!-- .fullwidth-block -->
    </main> <!-- .main-content -->
{% endblock %}
