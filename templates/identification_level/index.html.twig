{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <main class="main-content">
        <div class="fullwidth-block">
		</div> <!-- .fullwidth-block -->
        <div class="fullwidth-block">
            <div class="container">
                <div class="row">
                        <div class="card-body">
                            {% if identificationLevels | length > 0 %}
                                <h1 class="tomato-color">Identification Level List</h1>
                                <table class="table table-striped">
                                <thead>
                                    <tr>
                                    <th scope="col">Label</th>
                                    <th scope="col">Code</th>
                                    <th scope="col">Action</th>
                                    </tr>
                                </thead>
                                <tbody>
                                {% for identificationLevel in identificationLevels %}
                                    <tr>
                                        <td><a href="{{ path('identification_level_details', {id: identificationLevel.id}) }}">{{ identificationLevel.Label }}</a></td>
                                        <td><a href="{{ path('identification_level_details', {id: identificationLevel.id}) }}">{{ identificationLevel.code }}</a></td>
                                        <td>
                                            <a href="{{ path('identification_level_details', {id: identificationLevel.id}) }}"><button class="btn btn-outline-primary">View</button></a>
                                        </td>
                                    </tr>
                                {% endfor %}
                            {% else %} 
                                <p class="tomato-color">No identification level has been provided in our system</p>
                            {% endif %}
                        </tbody>
                        </table>
                     
                        </div>
                    </div>
                    </div>
                </div> <!-- .row -->
            </div> <!-- .container -->
        </div> <!-- .fullwidth-block -->
    </main> <!-- .main-content -->
{% endblock %}
