{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <main class="main-content">
        <div class="fullwidth-block">
		</div> <!-- .fullwidth-block -->

        <div class="fullwidth-block">
            <div class="container">
                <div class="row">
                        <div class="card-body">
                            <h1 class="tomato-color">Analyte Flavor & Health Edition</h1>
                            {{ form_start(flavorHealthForm) }}
                                {{ form_row(flavorHealthForm.name) }}
                                {{ form_row(flavorHealthForm.isActive) }}
                                <button type="submit" class="btn">Udpate</button>
                            {{ form_end(flavorHealthForm) }}
                        </div>
                    </div>
                    </div>
                </div> <!-- .row -->
            </div> <!-- .container -->
        </div> <!-- .fullwidth-block -->
    </main> <!-- .main-content -->
{% endblock %}
