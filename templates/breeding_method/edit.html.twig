{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <main class="main-content">
        <div class="fullwidth-block">
		</div> <!-- .fullwidth-block -->

        <div class="fullwidth-block">
            <div class="container">
                <div class="row">
                        <div class="card-body">
                            <h1 class="tomato-color">Breeding Method</h1>
                            {{ form_start(breedingMethodForm) }}
                                {{ form_row(breedingMethodForm.name) }}
                                {{ form_row(breedingMethodForm.description) }}
                                {{ form_row(breedingMethodForm.parentTerm) }}
                                {{ form_row(breedingMethodForm.ontology_id) }}
                                {{ form_row(breedingMethodForm.createdAt) }}
                                {{ form_row(breedingMethodForm.isActive) }}
                                <button type="submit" class="btn">Udpate</button>
                            {{ form_end(breedingMethodForm) }}
                        </div>
                    </div>
                    </div>
                </div> <!-- .row -->
            </div> <!-- .container -->
        </div> <!-- .fullwidth-block -->
    </main> <!-- .main-content -->
{% endblock %}
