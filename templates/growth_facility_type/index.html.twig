{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <main class="main-content">
        <div class="fullwidth-block">
		</div> <!-- .fullwidth-block -->
        <div class="fullwidth-block">
            <div class="container">
                <div class="row">
                        <div class="card-body">
                            {% if growthFacilityTypes | length > 0 %}
                                <h1 class="tomato-color">Growth Facility Type</h1>
                                <table class="table table-striped">
                                <thead>
                                    <tr>
                                    <th scope="col">Name</th>
                                    <th scope="col">Description</th>
                                    <th scope="col">Action</th>
                                    </tr>
                                </thead>
                                <tbody>
                                {% for growthFacilityType in growthFacilityTypes %}
                                    <tr>
                                        <td><a href="{{ path('growth_facility_type_details', {id: growthFacilityType.id}) }}">{{ growthFacilityType.name }}</a></td>
                                        <td><a href="{{ path('growth_facility_type_details', {id: growthFacilityType.id}) }}">{{ growthFacilityType.description }}</a></td>
                                        <td>
                                            <a href="{{ path('growth_facility_type_details', {id: growthFacilityType.id}) }}"><button class="btn btn-outline-primary">View</button></a>
                                        </td>
                                    </tr>
                                {% endfor %}
                            {% else %} 
                                <p class="tomato-color">No growth facility type has been provided in our system</p>
                            {% endif %}
                        </tbody>
                        </table>
                     
                        </div>
                    </div>
                    </div>
                </div> <!-- .row -->
            </div> <!-- .container -->
        </div> <!-- .fullwidth-block -->
    </main> <!-- .main-content -->
{% endblock %}
