{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <main class="main-content">
        <div class="fullwidth-block">
		</div> <!-- .fullwidth-block -->

        <div class="fullwidth-block">
            <div class="container">
                <div class="row">
                        <div class="card-body">
                            <h1 class="tomato-color">Sequencing Instrument Edition</h1>
                            {{ form_start(sequencingInstrumentForm) }}
                                {{ form_row(sequencingInstrumentForm.label) }}
                                {{ form_row(sequencingInstrumentForm.createdAt) }}
                                {{ form_row(sequencingInstrumentForm.isActive) }}
                                <button type="submit" class="btn">Udpate</button>
                            {{ form_end(sequencingInstrumentForm) }}
                        </div>
                    </div>
                    </div>
                </div> <!-- .row -->
            </div> <!-- .container -->
        </div> <!-- .fullwidth-block -->
    </main> <!-- .main-content -->
{% endblock %}
