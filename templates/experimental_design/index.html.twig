{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <main class="main-content">
        <div class="fullwidth-block">
		</div> <!-- .fullwidth-block -->
        <div class="fullwidth-block">
            <div class="container">
                <div class="row">
                        <div class="card-body">
                            {% if experimentalDesigns | length > 0 %}
                                <h1 class="tomato-color">Experimental Design</h1>
                                <table class="table table-striped">
                                <thead>
                                    <tr>
                                    <th scope="col">Name</th>
                                    <th scope="col">Description</th>
                                    <th scope="col">Action</th>
                                    </tr>
                                </thead>
                                <tbody>
                                {% for experimentalDesign in experimentalDesigns %}
                                    <tr>
                                        <td><a href="{{ path('experimental_design_details', {id: experimentalDesign.id}) }}">{{ experimentalDesign.name }}</a></td>
                                        <td><a href="{{ path('experimental_design_details', {id: experimentalDesign.id}) }}">{{ experimentalDesign.description }}</a></td>
                                        <td>
                                            <a href="{{ path('experimental_design_details', {id: experimentalDesign.id}) }}"><button class="btn btn-outline-primary">View</button></a>
                                        </td>
                                    </tr>
                                {% endfor %}
                            {% else %} 
                                <p class="tomato-color">No experimental design type has been provided in our system</p>
                            {% endif %}
                        </tbody>
                        </table>
                     
                        </div>
                    </div>
                    </div>
                </div> <!-- .row -->
            </div> <!-- .container -->
        </div> <!-- .fullwidth-block -->
    </main> <!-- .main-content -->
{% endblock %}
