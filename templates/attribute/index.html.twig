{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <main class="main-content">
        <div class="fullwidth-block">
		</div> <!-- .fullwidth-block -->
        <div class="fullwidth-block">
            <div class="container">
                <div class="row">
                        <div class="card-body">
                            {% if attributes | length > 0 %}
                                <h1 class="tomato-color">Attribute List</h1>
                                <table class="table table-striped">
                                <thead>
                                    <tr>
                                    <th scope="col">Name</th>
                                    <th scope="col">Abbreviation</th>
                                    <th scope="col">Description</th>
                                    <th scope="col">Category</th>
                                    <th scope="col">Action</th>
                                    </tr>
                                </thead>
                                <tbody>
                                {% for attribute in attributes %}
                                    <tr>
                                        <td><a href="{{ path('attribute_details', {id: attribute.id}) }}">{{ attribute.name }}</a></td>
                                        <td><a href="{{ path('attribute_details', {id: attribute.id}) }}">{{ attribute.abbreviation }}</a></td>
                                        <td><a href="{{ path('attribute_details', {id: attribute.id}) }}">{{ attribute.description }}</a></td>
                                        <td><a href="{{ path('attribute_details', {id: attribute.id}) }}">{{ attribute.category }}</a></td>
                                        <td>
                                            <a href="{{ path('attribute_details', {id: attribute.id}) }}"><button class="btn btn-outline-primary">View</button></a>
                                        </td>
                                    </tr>
                                {% endfor %}
                            {% else %} 
                                <p class="tomato-color">No attribute has been provided in our system</p>
                            {% endif %}
                        </tbody>
                        </table>
                     
                        </div>
                    </div>
                    </div>
                </div> <!-- .row -->
            </div> <!-- .container -->
        </div> <!-- .fullwidth-block -->
    </main> <!-- .main-content -->
{% endblock %}
