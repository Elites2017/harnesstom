{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <main class="main-content">
        <div class="fullwidth-block">
		</div> <!-- .fullwidth-block -->

        <div class="fullwidth-block">
            <div class="container">
                <div class="row">
                        <div class="card-body">
                            <h1 class="tomato-color">Attribute Category</h1>
                            {{ form_start(attributeCategoryForm) }}
                                {{ form_row(attributeCategoryForm.abbreviation) }}
                                {{ form_row(attributeCategoryForm.name) }}
                                {{ form_row(attributeCategoryForm.description) }}
                                {{ form_row(attributeCategoryForm.createdAt) }}
                                {{ form_row(attributeCategoryForm.isActive) }}
                                <button type="submit" class="btn">Udpate</button>
                            {{ form_end(attributeCategoryForm) }}
                        </div>
                    </div>
                    </div>
                </div> <!-- .row -->
            </div> <!-- .container -->
        </div> <!-- .fullwidth-block -->
    </main> <!-- .main-content -->
{% endblock %}
