{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <main class="main-content">
        <div class="fullwidth-block">
		</div> <!-- .fullwidth-block -->
        <div class="fullwidth-block">
            <div class="container">
                <div class="row">
                        <div class="card-body">
                            {% if genotypingPlatforms | length > 0 %}
                                <h1 class="tomato-color">Genotyping Platform List</h1>
                                <table class="table table-striped">
                                <thead>
                                    <tr>
                                    <th scope="col">Name</th>
                                    <th scope="col">Description</th>
                                    <th scope="col">Ref Set Name</th>
                                    <th scope="col">Data Type</th>
                                    <th scope="col">Unit</th>
                                    <th scope="col">Action</th>
                                    </tr>
                                </thead>
                                <tbody>
                                {% for genotypingPlatform in genotypingPlatforms %}
                                    <tr>
                                        <td><a href="{{ path('genotyping_platform_details', {id: genotypingPlatform.id}) }}">{{ genotypingPlatform.name }}</a></td>
                                        <td><a href="{{ path('genotyping_platform_details', {id: genotypingPlatform.id}) }}">{{ genotypingPlatform.description }}</a></td>
                                        <td><a href="{{ path('genotyping_platform_details', {id: genotypingPlatform.id}) }}">{{ genotypingPlatform.refSetName }}</a></td>
                                        <td><a href="{{ path('genotyping_platform_details', {id: genotypingPlatform.id}) }}">{{ genotypingPlatform.bioProjectID }}</a></td>
                                        <td><a href="{{ path('genotyping_platform_details', {id: genotypingPlatform.id}) }}">{{ genotypingPlatform.markerCount }}</a></td>
                                        <td>
                                            <a href="{{ path('genotyping_platform_details', {id: genotypingPlatform.id}) }}"><button class="btn btn-outline-primary">View</button></a>
                                        </td>
                                    </tr>
                                {% endfor %}
                            {% else %} 
                                <p class="tomato-color">No genotyping plaform has been provided in our system</p>
                            {% endif %}
                        </tbody>
                        </table>
                     
                        </div>
                    </div>
                    </div>
                </div> <!-- .row -->
            </div> <!-- .container -->
        </div> <!-- .fullwidth-block -->
    </main> <!-- .main-content -->
{% endblock %}
