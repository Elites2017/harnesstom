{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <main class="main-content">
        <div class="fullwidth-block">
		</div> <!-- .fullwidth-block -->

        <div class="fullwidth-block">
            <div class="container">
                <div class="row">
                        <div class="card-body">
                            <h1 class="tomato-color">Sequencing Type Edition</h1>
                            {{ form_start(sequencingTypeForm) }}
                                {{ form_row(sequencingTypeForm.label) }}
                                {{ form_row(sequencingTypeForm.createdAt) }}
                                {{ form_row(sequencingTypeForm.isActive) }}
                                <button type="submit" class="btn">Udpate</button>
                            {{ form_end(sequencingTypeForm) }}
                        </div>
                    </div>
                    </div>
                </div> <!-- .row -->
            </div> <!-- .container -->
        </div> <!-- .fullwidth-block -->
    </main> <!-- .main-content -->
{% endblock %}
