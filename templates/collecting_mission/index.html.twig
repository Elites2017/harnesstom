{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <main class="main-content">
        <div class="fullwidth-block">
		</div> <!-- .fullwidth-block -->
        <div class="fullwidth-block">
            <div class="container">
                <div class="row">
                        <div class="card-body">
                            {% if collectingMissions | length > 0 %}
                                <h1 class="tomato-color">Collecting Mission List</h1>
                                <table class="table table-striped">
                                <thead>
                                    <tr>
                                    <th scope="col">Name</th>
                                    <th scope="col">Species</th>
                                    <th scope="col">Institue</th>
                                    </tr>
                                </thead>
                                <tbody>
                                {% for collectingMission in collectingMissions %}
                                    <tr>
                                        <td><a href="{{ path('collecting_mission_details', {id: collectingMission.id}) }}">{{ collectingMission.name }}</a></td>
                                        <td><a href="{{ path('collecting_mission_details', {id: collectingMission.id}) }}">{{ collectingMission.species }}</a></td>
                                        <td><a href="{{ path('collecting_mission_details', {id: collectingMission.id}) }}">{{ collectingMission.institute }}</a></td>
                                       <td>
                                            <a href="{{ path('collecting_mission_details', {id: collectingMission.id}) }}"><button class="btn btn-outline-primary">View</button></a>
                                        </td>
                                    </tr>
                                {% endfor %}
                            {% else %} 
                                <p class="tomato-color">No collecting mission has been provided in our system</p>
                            {% endif %}
                        </tbody>
                        </table>
                     
                        </div>
                    </div>
                    </div>
                </div> <!-- .row -->
            </div> <!-- .container -->
        </div> <!-- .fullwidth-block -->
    </main> <!-- .main-content -->
{% endblock %}
