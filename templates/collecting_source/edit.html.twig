{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <main class="main-content">
        <div class="fullwidth-block">
		</div> <!-- .fullwidth-block -->

        <div class="fullwidth-block">
            <div class="container">
                <div class="row">
                        <div class="card-body">
                            <h1 class="tomato-color">Collecting SOurce</h1>
                            {{ form_start(collectingSourceForm) }}
                                {{ form_row(collectingSourceForm.label) }}
                                {{ form_row(collectingSourceForm.code) }}
                                {{ form_row(collectingSourceForm.parentTerm) }}
                                {{ form_row(collectingSourceForm.createdAt) }}
                                {{ form_row(collectingSourceForm.isActive) }}
                                <button type="submit" class="btn">Udpate</button>
                            {{ form_end(collectingSourceForm) }}
                        </div>
                    </div>
                    </div>
                </div> <!-- .row -->
            </div> <!-- .container -->
        </div> <!-- .fullwidth-block -->
    </main> <!-- .main-content -->
{% endblock %}
