{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <main class="main-content">
        <div class="fullwidth-block">
		</div> <!-- .fullwidth-block -->
        <div class="fullwidth-block">
            <div class="container">
                <div class="row">
                        <div class="card-body">
                            {% if collectingSources | length > 0 %}
                                <h1 class="tomato-color">Anatomical Entity List</h1>
                                <table class="table table-striped">
                                <thead>
                                    <tr>
                                    <th scope="col">Label</th>
                                    <th scope="col">Code</th>
                                    <th scope="col">Action</th>
                                    </tr>
                                </thead>
                                <tbody>
                                {% for collectingSource in collectingSources %}
                                    <tr>
                                        <td><a href="{{ path('collecting_source_details', {id: collectingSource.id}) }}">{{ collectingSource.Label }}</a></td>
                                        <td><a href="{{ path('collecting_source_details', {id: collectingSource.id}) }}">{{ collectingSource.code }}</a></td>
                                        <td>
                                            <a href="{{ path('collecting_source_details', {id: collectingSource.id}) }}"><button class="btn btn-outline-primary">View</button></a>
                                        </td>
                                    </tr>
                                {% endfor %}
                            {% else %} 
                                <p class="tomato-color">No collecting source has been provided in our system</p>
                            {% endif %}
                        </tbody>
                        </table>
                     
                        </div>
                    </div>
                    </div>
                </div> <!-- .row -->
            </div> <!-- .container -->
        </div> <!-- .fullwidth-block -->
    </main> <!-- .main-content -->
{% endblock %}
