{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <main class="main-content">
        <div class="fullwidth-block">
		</div> <!-- .fullwidth-block -->
        <div class="fullwidth-block">
            <div class="container">
                <div class="row">
                        <div class="card-body">
                            {% if crops | length > 0 %}
                                <h1 class="tomato-color">crops list</h1>
                                <table class="table table-striped">
                                <thead>
                                    <tr>
                                    <th scope="col">Crop name</th>
                                    <th scope="col">Action</th>
                                    </tr>
                                </thead>
                                <tbody>
                                {% for crop in crops %}
                                    <tr>
                                        <td><a href="{{ path('crop_details', {id: crop.id}) }}">{{ crop.commonCropName }}</a></td>
                                        <td>
                                            <a href="{{ path('crop_details', {id: crop.id}) }}"><button class="btn btn-outline-primary">View</button></a>
                                        </td>
                                    </tr>
                                {% endfor %}
                            {% else %} 
                                <p class="tomato-color">No crop has been provided in our system</p>
                            {% endif %}
                        </tbody>
                        </table>
                     
                        </div>
                    </div>
                    </div>
                </div> <!-- .row -->
            </div> <!-- .container -->
        </div> <!-- .fullwidth-block -->
    </main> <!-- .main-content -->
{% endblock %}
