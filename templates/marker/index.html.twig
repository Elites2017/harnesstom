{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <main class="main-content">
        <div class="fullwidth-block">
		</div> <!-- .fullwidth-block -->
        <div class="fullwidth-block">
            <div class="container">
                <div class="row">
                        <div class="card-body">
                            {% if markers | length > 0 %}
                                <h1 class="tomato-color">Marker List</h1>
                                <table class="table table-striped">
                                <thead>
                                    <tr>
                                    <th scope="col">Name</th>
                                    <th scope="col">Type</th>
                                    <th scope="col">Linkage Groupe Name</th>
                                    <th scope="col">Position</th>
                                    <th scope="col">Action</th>
                                    </tr>
                                </thead>
                                <tbody>
                                {% for marker in markers %}
                                    <tr>
                                        <td><a href="{{ path('marker_details', {id: marker.id}) }}">{{ marker.name }}</a></td>
                                        <td><a href="{{ path('marker_details', {id: marker.id}) }}">{{ marker.type }}</a></td>
                                        <td><a href="{{ path('marker_details', {id: marker.id}) }}">{{ marker.linkageGroupName }}</a></td>
                                        <td><a href="{{ path('marker_details', {id: marker.id}) }}">{{ marker.position }}</a></td>
                                        <td>
                                            <a href="{{ path('marker_details', {id: marker.id}) }}"><button class="btn btn-outline-primary">View</button></a>
                                        </td>
                                    </tr>
                                {% endfor %}
                            {% else %} 
                                <p class="tomato-color">No marker has been provided in our system</p>
                            {% endif %}
                        </tbody>
                        </table>
                     
                        </div>
                    </div>
                    </div>
                </div> <!-- .row -->
            </div> <!-- .container -->
        </div> <!-- .fullwidth-block -->
    </main> <!-- .main-content -->
{% endblock %}
